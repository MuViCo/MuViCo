services:
  app:
    build:
      context: ./
      dockerfile: Containerfile
    image: dev
    volumes:
      - ./:/opt/app-root/src
    ports:
      - 3000:3000
      - 8000:8000
    container_name: dev
  
volumes:
  mongo_data:
